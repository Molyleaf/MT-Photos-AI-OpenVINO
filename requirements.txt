numpy==2.3.5
insightface
fastapi
pydantic
openvino
# RapidOCR 3.7.0 runs with the upstream OpenVINO CPU path, repo-managed lazy-load/admission/timeout controls, idle non-text release, and no local session replacement in app/models.py.
rapidocr==3.7.0
onnxruntime-openvino
uvicorn
python-multipart
pyyaml
six
