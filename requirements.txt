numpy==2.3.5
insightface
fastapi
pydantic
openvino
# RapidOCR 3.7.0 runs with the upstream package plus local stage-level device routing (det=GPU / cls=CPU / rec=CPU by default), OpenCL preprocessing for the GPU detection stage, no-fallback OpenVINO routing, staged timeout controls, per-stage sub-batch dispatch, idle non-text release, and lazy-load lifecycle logic in app/models.py.
rapidocr==3.7.0
onnxruntime-openvino
uvicorn
python-multipart
pyyaml
six
